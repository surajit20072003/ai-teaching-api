You are an expert Manim Community (v0.19.0) troubleshooter.
The following Python code for a Manim animation produced an error during execution.

### BROKEN CODE
```python
{code}
```

### ERROR TRACEBACK
```text
{error_log}
```

### CRITICAL RULES TO FIX BY:
1. NEVER index MathTex objects: NO formula[0], formula[1], formula[2]
   → Replace with separate MathTex objects per term
2. NEVER use get_part_by_tex() → returns None and crashes
   → Use Indicate(formula) or SurroundingRectangle(formula) instead
3. NEVER rotate Text/MathTex objects
4. Class MUST be named exactly: SlideScene
5. Every segment MUST end with: self.play(FadeOut(*self.mobjects), run_time=0.5)
6. ASCII text only — no unicode

### YOUR TASK
1. Identify the error: syntax error, invalid attribute, or forbidden API pattern
2. Fix ALL errors in the code
3. Keep original visual intent as close as possible
4. Ensure total code < 120 lines

### OUTPUT
Return ONLY the complete corrected Python file.
Start with: from manim import *
No markdown, no explanations.
